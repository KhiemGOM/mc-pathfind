// Root project has no sources of its own -- see settings.gradle.kts for the
// actual modules (core, benchmark, pearl). Each module declares its own
// dependencies in its own build.gradle.kts; repositories are centralized in
// settings.gradle.kts's dependencyResolutionManagement block.
